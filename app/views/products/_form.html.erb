<%= semantic_form_for [:user, @product] do |f| %>
  <%= f.inputs do %>
    <%= f.input :name %>
    <%= f.input :price %>
    <%= f.input :image, as: :file %>
    <%= f.input :active unless @product.new_record? %>
    <%= f.actions %>
  <% end  %>
<% end  %>